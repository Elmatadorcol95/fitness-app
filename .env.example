# Copia este archivo a .env y rellena los valores reales.
# NUNCA pegues valores de credenciales en un prompt interactivo de
# terminal (ni aqui, ni en eas env:create) — usa flags no interactivos o
# copia/pega desde el dashboard de origen.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
